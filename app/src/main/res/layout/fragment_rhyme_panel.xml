<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/surface"
    android:maxHeight="600dp"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- 标题 -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:text="@string/rhyme_suggest"
        android:textColor="@color/text_primary"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- 输入框 -->
    <EditText
        android:id="@+id/et_rhyme_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/edit_line_bg"
        android:hint="@string/rhyme_input_hint"
        android:inputType="text"
        android:maxLines="1"
        android:padding="12dp" />

    <!-- 押韵类型选择 -->
    <RadioGroup
        android:id="@+id/rg_rhyme_type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rb_single"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:checked="true"
            android:text="@string/rhyme_single" />

        <RadioButton
            android:id="@+id/rb_double"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/rhyme_double" />
    </RadioGroup>

    <!-- 搜索按钮 -->
    <Button
        android:id="@+id/btn_rhyme_search"
        style="@style/Widget.Button.Primary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/rhyme_search" />

    <!-- 推荐词列表 -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_rhyme_results"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:maxHeight="300dp" />

    <!-- 空状态 -->
    <TextView
        android:id="@+id/tv_rhyme_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="无匹配结果"
        android:textColor="@color/text_hint"
        android:visibility="gone" />

</LinearLayout>
